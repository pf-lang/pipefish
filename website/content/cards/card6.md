## And superglue!

Pipefish has a uniform way of embedding existing or new DSLs in your code: this was used to write the `sql`, `prolog`, `html`, `json`, `http`, `smtp` and `exec` standard libraries, and can be used just as easily in userspace for whatever you like.